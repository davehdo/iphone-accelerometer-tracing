<%= form_for(@montage) do |f| %>
  <% if @montage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@montage.errors.count, "error") %> prohibited this montage from being saved:</h2>

      <ul>
      <% @montage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
